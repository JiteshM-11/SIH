{
  "name": "hackathon-backend",
  "version": "1.0.0",
  "description": "Co-Pilot Swarm backend: auth (JWT + bcrypt) and persistence (SQLite) for the swarm simulation.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  }
}
